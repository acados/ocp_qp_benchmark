
name: Benchmark test


on:
  push:
  pull_request:


jobs:
  cli_tests:
    name: CLI Commands Tests
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          path: ${{github.workspace}}/ocp_qp_benchmark
          submodules: 'recursive'
      - name: Setup environment
        uses: ./ocp_qp_benchmark/.github/actions/setup-acados
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install current project
        working-directory: ${{github.workspace}}/ocp_qp_benchmark
        run: |
          pip install -e .
      - name: Verify CLI Tools
        working-directory: ${{github.workspace}}/ocp_qp_benchmark
        run: |
          echo "Testing ocp-benchmark CLI..."
          ocp-benchmark --help
          echo "Testing add-problems CLI..."
          add-problems --help

  benchmark_tests:
    name: Benchmark Tests
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          path: ${{github.workspace}}/ocp_qp_benchmark
          submodules: 'recursive'
      - name: Setup environment
        uses: ./ocp_qp_benchmark/.github/actions/setup-acados
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install current project
        working-directory: ${{github.workspace}}/ocp_qp_benchmark
        run: |
          pip install -e .
      - name: Run benchmark with designated problems and designated solvers
        working-directory: ${{github.workspace}}/ocp_qp_benchmark
        run: |
          echo "Running ocp-benchmark to verify it executes..."
          ocp-benchmark -c tests/benchmark.json
      - name: Test IPOPT as reference solver
        working-directory: ${{github.workspace}}/ocp_qp_benchmark
        run: |
          echo "Testing IPOPT as reference solver..."
          ocp-benchmark -c tests/ref_test.json
      - name: Run single problem test
        working-directory: ${{github.workspace}}/ocp_qp_benchmark
        run: |
          echo "Running single problem test..."
          python tests/test_single_problem.py